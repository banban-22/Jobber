<h1> Forgot Passsword </h1>

<%= form_with(url: password_resets_path, method: :post) do |f| %>
  <p>Enter your email address below, and we'll send you instructions on how to reset your password.</p>

  <div class="d-flex">
  <%= f.email_field :email, placeholder: 'john@doe.com', class: "form-control w-25" %>

  <%= f.submit 'Send', class: "btn btn-primary ml-3 badge_gap" %>
  </div>
<% end %>